'use client';
import { useMemo, useState } from 'react';
import Link from 'next/link';
import { BarChart3, Bell, Building2, CheckCircle2, ChevronRight, FileUp, GraduationCap, LocateFixed, LogOut, Mic, Send, ShieldCheck, Sparkles, UserRound, Users, Wallet } from 'lucide-react';
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
import type { Domain, Problem, Project, Role } from '@/lib/types';
import CollaboratorFeed from '@/components/collaborator-feed';
import LogoutButton from '@/components/logout-button';

const seedProblems: Problem[] = [
 { id:'P-1042', title:'Contaminated drinking water near Angara', description:'Hand pumps are producing discoloured water and residents report illness.', domain:'Water Management', urgency_score:5, summary:'Unsafe drinking water is affecting Angara households.', recommended_university:'BIT Mesra, Ranchi', recommended_department:'Department of Civil and Environmental Engineering', ai_matching_reason:'BIT Mesra has water quality research and nearby field access.', status:'Submitted', created_at:'Today, 10:32 AM' },
 { id:'P-1041', title:'Crop disease damaging paddy fields', description:'Brown spots are spreading across a village paddy crop.', domain:'Agriculture', urgency_score:4, summary:'A potential fungal disease threatens paddy yield.', recommended_university:'Birsa Agricultural University', recommended_department:'Department of Plant Pathology', ai_matching_reason:'BAU specialises in regional crop diagnostics.', status:'Routed_To_University', assigned_university:'Birsa Agricultural University', created_at:'Yesterday' },
 { id:'P-1040', title:'Unsafe school approach road', description:'Children walk through a flooded, damaged road to reach school.', domain:'Infrastructure', urgency_score:3, summary:'A school access route needs a flood-resilient redesign.', recommended_university:'NIT Jamshedpur', recommended_department:'Department of Civil Engineering', ai_matching_reason:'NIT Jamshedpur can prototype resilient road solutions.', status:'In_Development', assigned_university:'NIT Jamshedpur', created_at:'18 Aug' }
];
const seedProjects: Project[] = [{ id:'PR-203', problem_id:'P-1040', title:'Flood-resilient school access', university_name:'NIT Jamshedpur', faculty_lead:'Dr. R. Kumar', student_team:'Aditi (22CE01), Manav (22CE11), Sana (22CE14)', funding_raised:75000, funding_target:150000, status:'Prototype design' }, { id:'PR-202', problem_id:'P-1041', title:'Low-cost paddy disease diagnostic', university_name:'Birsa Agricultural University', faculty_lead:'Dr. Neha Singh', student_team:'Agri Innovation Cell', funding_raised:30000, funding_target:100000, status:'Field validation' }];
const roles: { key:Role; label:string; icon:typeof Users }[] = [{key:'citizen',label:'Citizen',icon:Users},{key:'govt_admin',label:'Govt Admin',icon:ShieldCheck},{key:'university',label:'University',icon:GraduationCap},{key:'industry_csr',label:'Industry CSR',icon:Building2}];
const colors: Record<string,string> = {'Water Management':'#1f8b9c', Agriculture:'#83b735', Infrastructure:'#f0a53b', Healthcare:'#d9667c', Education:'#715bc7'};

export default function Portal({ initialRole = 'citizen' }: { initialRole?: Role }) {
 const role=initialRole; const [problems,setProblems]=useState(seedProblems); const [projects,setProjects]=useState(seedProjects); const [notice,setNotice]=useState('');
 const createProblem=(p:Problem)=>{setProblems(x=>[p,...x]); setNotice('Your challenge has been logged and AI-matched.');};
 const route=(id:string)=>{setProblems(x=>x.map(p=>p.id===id?{...p,status:'Routed_To_University',assigned_university:p.recommended_university}:p));setNotice('Challenge approved and routed to the recommended institution.');};
 const accept=(id:string, data:{faculty:string; students:string; target:number})=>{const problem=problems.find(p=>p.id===id)!;setProblems(x=>x.map(p=>p.id===id?{...p,status:'In_Development',assigned_university:problem.recommended_university}:p));setProjects(x=>[{id:`PR-${Date.now().toString().slice(-4)}`,problem_id:id,title:problem.title,university_name:problem.recommended_university,faculty_lead:data.faculty,student_team:data.students,funding_raised:0,funding_target:data.target,status:'Research planning'},...x]);setNotice('Challenge accepted — project workspace created.');};
 const sponsor=(id:string)=>{setProjects(x=>x.map(p=>p.id===id?{...p,funding_raised:Math.min(p.funding_target,p.funding_raised+50000)}:p));setNotice('₹50,000 mock CSR contribution recorded. Thank you!');};
 return <main className="min-h-screen"><header className="sticky top-0 z-20 border-b border-white/70 bg-mist/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-3"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-moss text-xl font-black text-lime">S</div><div><p className="font-bold leading-none">Sahaay</p><p className="text-xs text-slate-500">Societal Innovation Portal</p></div></div><div className="flex items-center gap-2"><button className="btn bg-white" aria-label="Notifications"><Bell className="h-4 w-4"/></button><button className="btn bg-white" aria-label="Profile"><UserRound className="h-4 w-4"/></button><span className="hidden rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-600 md:block">Secure role workspace</span><Link href="/" className="btn bg-ink text-white" aria-label="Logout"><LogOut className="h-4 w-4"/><span className="hidden md:inline">Logout</span></Link></div></div></header>
 <section className="mx-auto max-w-7xl px-5 py-8"><div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-sm font-bold uppercase tracking-[.18em] text-moss">One challenge. Many collaborators.</p><h1 className="text-3xl font-black tracking-tight md:text-5xl">Turn local problems into <span className="text-moss">shared progress.</span></h1></div><div className="flex items-center gap-2 rounded-2xl bg-white px-4 py-3 text-sm shadow-sm"><Sparkles className="h-5 w-5 text-moss"/> AI-assisted routing is active</div></div>{notice && <div className="mb-5 flex items-center justify-between rounded-xl border border-moss/20 bg-lime/40 px-4 py-3 text-sm font-semibold"><span>{notice}</span><button onClick={()=>setNotice('')} aria-label="Dismiss">×</button></div>}
 <CollaboratorFeed role={role}/>{role==='citizen'&&<Citizen problems={problems} onCreate={createProblem}/>} {role==='govt_admin'&&<Government problems={problems} onRoute={route}/>} {role==='university'&&<University problems={problems} onAccept={accept}/>} {role==='industry_csr'&&<Industry projects={projects} onSponsor={sponsor}/>}<Analytics problems={problems} projects={projects}/></section></main>;
}
function RoleTabs({role,setRole}:{role:Role;setRole:(r:Role)=>void}) { return <nav className="mb-6 flex gap-2 overflow-x-auto pb-1">{roles.map(({key,label,icon:Icon})=><button key={key} onClick={()=>setRole(key)} className={`btn whitespace-nowrap ${role===key?'bg-ink text-white':'bg-white text-slate-600'}`}><Icon className="h-4 w-4"/>{label}</button>)}</nav> }
function Citizen({problems,onCreate}:{problems:Problem[];onCreate:(p:Problem)=>void}) { const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [location,setLocation]=useState<{lat:number;lng:number}|null>(null);const [media,setMedia]=useState('');const [busy,setBusy]=useState(false); const geo=()=>navigator.geolocation?.getCurrentPosition(p=>setLocation({lat:p.coords.latitude,lng:p.coords.longitude}),()=>alert('Location access was not available.')); const speak=()=>{const Recognition=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!Recognition)return alert('Speech recognition is not supported by this browser.');const r=new Recognition();r.lang='en-IN';r.onresult=(e:any)=>setDescription(x=>`${x} ${e.results[0][0].transcript}`.trim());r.start();}; const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!title||!description)return;setBusy(true);let ai:any;try{ai=await fetch('/api/classify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({description,location})}).then(r=>r.json());}catch{ai={domain:'Infrastructure',urgency_score:3,summary:'Issue submitted for assessment.',recommended_university:'BIT Mesra, Ranchi',recommended_department:'Engineering',ai_matching_reason:'Awaiting review.'};}onCreate({id:`P-${Date.now().toString().slice(-4)}`,title,description,location_lat:location?.lat,location_lng:location?.lng,media_url:media,...ai,status:'Submitted',created_at:'Just now'});setTitle('');setDescription('');setMedia('');setBusy(false);}; return <div className="grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><form onSubmit={submit} className="card p-6"><h2 className="text-xl font-bold">Report a local challenge</h2><p className="mb-5 text-sm text-slate-500">Your report is classified and matched with a capable institution.</p><label className="label">Issue title</label><input className="input mb-4" value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Drinking water has a metallic smell"/><label className="label">Describe what is happening</label><textarea className="input mb-3 min-h-32" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Include who is affected, when it started, and what you observed."/><div className="mb-5 flex flex-wrap gap-2"><button type="button" onClick={speak} className="btn bg-slate-100"><Mic className="h-4 w-4"/> Speak your report</button><button type="button" onClick={geo} className={`btn ${location?'bg-lime':'bg-slate-100'}`}><LocateFixed className="h-4 w-4"/>{location?'Location attached':'Capture location'}</button><label className="btn cursor-pointer bg-slate-100"><FileUp className="h-4 w-4"/> Add photo, video or PDF<input className="hidden" type="file" accept="image/*,video/*,.pdf" onChange={e=>setMedia(e.target.files?.[0]?.name||'')}/></label></div>{media&&<p className="mb-4 text-xs text-moss">Attached: {media}</p>}<button disabled={busy} className="btn-primary w-full">{busy?'Finding the best match…':'Submit for AI matching'} <Send className="h-4 w-4"/></button></form><div className="card p-6"><p className="text-sm font-bold text-moss">MY TRACKER</p><h2 className="mb-5 text-xl font-bold">Your recent submissions</h2>{problems.slice(0,3).map(p=><Ticket key={p.id} p={p}/>)}</div></div> }
function Ticket({p}:{p:Problem}) { return <article className="mb-3 rounded-2xl border p-4 last:mb-0"><div className="mb-2 flex items-start justify-between gap-2"><h3 className="font-semibold">{p.title}</h3><Urgency n={p.urgency_score}/></div><div className="flex items-center justify-between text-xs"><span className="rounded-full bg-slate-100 px-2 py-1 font-medium">{p.domain}</span><span className="font-semibold text-moss">{p.status.replaceAll('_',' ')}</span></div></article> }
function Urgency({n}:{n:number}) { return <span className={`rounded-full px-2 py-1 text-xs font-bold ${n>=4?'bg-red-100 text-red-700':n===3?'bg-amber-100 text-amber-700':'bg-emerald-100 text-emerald-700'}`}>Urgency {n}/5</span> }
function Government({problems,onRoute}:{problems:Problem[];onRoute:(id:string)=>void}) { const [selected,setSelected]=useState(problems[0]?.id);const p=problems.find(x=>x.id===selected)||problems[0];return <div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="card overflow-hidden"><div className="border-b p-5"><h2 className="font-bold">Incoming challenge queue</h2><p className="text-sm text-slate-500">Prioritised by AI urgency</p></div>{[...problems].sort((a,b)=>b.urgency_score-a.urgency_score).map(x=><button key={x.id} onClick={()=>setSelected(x.id)} className={`w-full border-b p-4 text-left last:border-0 ${selected===x.id?'bg-lime/30':''}`}><div className="mb-2 flex justify-between gap-2"><span className="font-semibold">{x.title}</span><Urgency n={x.urgency_score}/></div><span className="text-xs text-slate-500">{x.domain} · {x.created_at}</span></button>)}</div>{p&&<div className="card p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><span className="text-xs font-bold uppercase tracking-widest text-moss">{p.id} · {p.domain}</span><h2 className="text-2xl font-bold">{p.title}</h2></div><Urgency n={p.urgency_score}/></div><p className="mb-6 text-slate-600">{p.description}</p><div className="rounded-2xl bg-ink p-5 text-white"><div className="mb-3 flex items-center gap-2 font-bold text-lime"><Sparkles className="h-5 w-5"/> AI Match Recommendation</div><p className="text-lg font-bold">{p.recommended_university}</p><p className="mb-3 text-sm text-slate-300">{p.recommended_department}</p><p className="text-sm text-slate-200">{p.ai_matching_reason}</p></div><button disabled={p.status!=='Submitted'} onClick={()=>onRoute(p.id)} className="btn-primary mt-5">{p.status==='Submitted'?'Approve & Route':'Already '+p.status.replaceAll('_',' ')} <ChevronRight className="h-4 w-4"/></button></div>}</div> }
function University({problems,onAccept}:{problems:Problem[];onAccept:(id:string,d:{faculty:string;students:string;target:number})=>void}) { const [university,setUniversity]=useState('BIT Mesra, Ranchi');const [active,setActive]=useState<Problem|null>(null); const tickets=problems.filter(p=>p.recommended_university===university&&p.status==='Routed_To_University'); return <div><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-bold">University challenge workspace</h2><p className="text-slate-500">Accept challenges and mobilise your department.</p></div><select className="input w-auto" value={university} onChange={e=>setUniversity(e.target.value)}>{['BIT Mesra, Ranchi','Birsa Agricultural University','NIT Jamshedpur'].map(x=><option key={x}>{x}</option>)}</select></div><div className="grid gap-4 md:grid-cols-2">{tickets.length?tickets.map(p=><article key={p.id} className="card p-5"><div className="mb-3 flex justify-between"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{p.domain}</span><Urgency n={p.urgency_score}/></div><h3 className="text-lg font-bold">{p.title}</h3><p className="my-3 text-sm text-slate-500">Department: {p.recommended_department}</p><button className="btn-soft" onClick={()=>setActive(p)}><CheckCircle2 className="h-4 w-4"/> Accept challenge</button></article>):<div className="card col-span-full p-10 text-center text-slate-500">No routed challenges for this university yet.</div>}</div>{active&&<AcceptModal p={active} close={()=>setActive(null)} onAccept={d=>{onAccept(active.id,d);setActive(null)}}/>}</div> }
function AcceptModal({p,close,onAccept}:{p:Problem;close:()=>void;onAccept:(d:{faculty:string;students:string;target:number})=>void}) { const [faculty,setFaculty]=useState('');const [students,setStudents]=useState('');const [target,setTarget]=useState('100000');return <div className="fixed inset-0 z-30 grid place-items-center bg-ink/40 p-4"><form onSubmit={e=>{e.preventDefault();onAccept({faculty,students,target:Number(target)})}} className="card w-full max-w-lg p-6"><div className="mb-4 flex justify-between gap-4"><div><p className="text-xs font-bold text-moss">ACCEPT CHALLENGE</p><h3 className="text-xl font-bold">{p.title}</h3></div><button type="button" onClick={close}>×</button></div><label className="label">Faculty mentor name</label><input required className="input mb-3" value={faculty} onChange={e=>setFaculty(e.target.value)} placeholder="Dr. Name"/><label className="label">Students and roll numbers</label><textarea required className="input mb-3" value={students} onChange={e=>setStudents(e.target.value)} placeholder="Name (roll no.), Name (roll no.)"/><label className="label">Estimated project budget (₹)</label><input required min="1" type="number" className="input mb-5" value={target} onChange={e=>setTarget(e.target.value)}/><div className="flex justify-end gap-2"><button type="button" className="btn bg-slate-100" onClick={close}>Cancel</button><button className="btn-primary">Create project</button></div></form></div> }
function Industry({projects,onSponsor}:{projects:Project[];onSponsor:(id:string)=>void}) { return <div><div className="mb-5"><h2 className="text-2xl font-bold">CSR opportunity marketplace</h2><p className="text-slate-500">Back applied university research that delivers measurable local impact.</p></div><div className="grid gap-5 md:grid-cols-2">{projects.map(p=>{const progress=Math.round(p.funding_raised/p.funding_target*100);return <article key={p.id} className="card p-6"><div className="mb-4 flex items-center justify-between"><span className="rounded-full bg-lime px-3 py-1 text-xs font-bold">{p.status}</span><span className="text-xs text-slate-500">{p.university_name}</span></div><h3 className="text-xl font-bold">{p.title}</h3><p className="mt-2 text-sm text-slate-500">Led by {p.faculty_lead}</p><div className="my-5"><div className="mb-2 flex justify-between text-sm font-semibold"><span>₹{p.funding_raised.toLocaleString('en-IN')} raised</span><span>{progress}%</span></div><div className="h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-moss" style={{width:`${progress}%`}}/></div><p className="mt-2 text-xs text-slate-500">Target: ₹{p.funding_target.toLocaleString('en-IN')}</p></div><button className="btn-primary" disabled={p.funding_raised>=p.funding_target} onClick={()=>onSponsor(p.id)}><Wallet className="h-4 w-4"/> Sponsor ₹50,000</button></article>})}</div></div> }
function Analytics({problems,projects}:{problems:Problem[];projects:Project[]}) { const data=useMemo(()=>Object.entries(problems.reduce((a,p)=>({...a,[p.domain]:(a[p.domain]||0)+1}),{} as Record<Domain,number>)).map(([name,value])=>({name,value})),[problems]);const raised=projects.reduce((s,p)=>s+p.funding_raised,0);return <section className="mt-10 border-t pt-8"><div className="mb-5 flex items-center gap-2"><BarChart3 className="h-5 w-5 text-moss"/><div><h2 className="text-2xl font-bold">State innovation pulse</h2><p className="text-sm text-slate-500">Live view across reported community challenges.</p></div></div><div className="grid gap-5 md:grid-cols-3"><div className="card h-64 p-4"><p className="text-sm font-bold">Sector distribution</p><ResponsiveContainer width="100%" height="90%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={48} outerRadius={76}>{data.map((d,i)=><Cell key={i} fill={colors[d.name]||'#64748b'}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div><div className="card p-6"><p className="text-sm font-bold">Urgency heat indicator</p><div className="mt-7 flex items-end gap-2">{[1,2,3,4,5].map(n=>{const count=problems.filter(p=>p.urgency_score===n).length;return <div key={n} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-lg bg-moss" style={{height:`${Math.max(16,count*36)}px`,opacity:.35+n/10}}/><span className="text-xs font-bold">{n}</span></div>})}</div><p className="mt-6 text-sm text-slate-500">Higher bars indicate more challenges at that urgency level.</p></div><div className="card p-6"><p className="text-sm font-bold">Collaboration outcomes</p><div className="mt-5 space-y-4"><Metric label="University allocations" value={new Set(problems.filter(p=>p.assigned_university).map(p=>p.assigned_university)).size.toString()} icon={<GraduationCap/>}/><Metric label="CSR funds raised" value={`₹${raised.toLocaleString('en-IN')}`} icon={<Wallet/>}/><Metric label="Challenges in development" value={problems.filter(p=>p.status==='In_Development').length.toString()} icon={<CheckCircle2/>}/></div></div></div></section> }
function Metric({label,value,icon}:{label:string;value:string;icon:React.ReactNode}) { return <div className="flex items-center gap-3"><div className="rounded-xl bg-lime p-2 text-moss">{icon}</div><div><p className="text-lg font-black">{value}</p><p className="text-xs text-slate-500">{label}</p></div></div> }
